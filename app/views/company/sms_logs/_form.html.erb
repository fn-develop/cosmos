<%= form_with(model: sms_log, local: true) do |form| %>
  <% if sms_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sms_log.errors.count, "error") %> prohibited this sms_log from being saved:</h2>

      <ul>
        <% sms_log.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
